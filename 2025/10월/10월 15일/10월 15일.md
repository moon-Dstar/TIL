# [TIL] Final Project

## React

### useEffect

#### Case 1
```
useEffect(() => {
        if (editing) {
            axios.get(`http://localhost:3001/posts/${id}`).then((res) => {
                setTitle(res.data.title);
                setBody(res.data.body);
            });
        }
    }, [id, editing]);
```

#### Case 2
```
    // [] 빈 배열: 단 한번만 호출
    useEffect(() => {
        getPosts();
    }, []);
```
